# TASKS

Last updated: 2026-04-22

## Status Keys

- `done`
- `in_progress`
- `todo`
- `blocked`

## 1. Completed

- [done] App versioning policy is defined: `pubspec.yaml` is the single source of truth, with scripted bump flow through `tool/bump_version.dart`.
- [done] Release preparation automation added: local `tool/prepare_release.sh` plus GitHub Actions workflow `.github/workflows/release-version.yml`.
- [done] Release tooling extended with autogenerated commit message, templated GitHub release notes, and tag-based Android/iOS build workflow.
- [done] Added branch-driven full automation: `main` auto-release prepare+mirror, `app` auto-release prepare+mobile deploy.
- [done] Added local `tool/dev_commit.sh` flow for `dev` branch commits without touching `pubspec.yaml` by default; optional `--bump-build` remains available for explicit development build bumps.
- [done] Added shared `ServerAvailabilityProvider` contract as the base abstraction for bootstrap/relay/turn health providers.
- [done] Added shared `ServerHealthCoordinator` so runtime startup and Settings reuse the same bootstrap/relay/turn health services and availability state.
- [done] Wired `HttpRelayClient` and `TurnAllocator` to shared coordinator-backed relay/turn health snapshots, with runtime-local fallback when shared health is still unknown.
- [done] Added shared health refresh on app resume and connectivity changes, so bootstrap/relay/turn availability is refreshed proactively in runtime.
- [done] Added selective relay-shortlist refresh before critical send/blob/fetch work when the shared relay health snapshot is stale.
- [done] Added selective TURN-shortlist refresh before building TURN-based call configs when the shared TURN health snapshot is stale.
- [done] Automated release notes generation now prefers the changelog section for the target version and falls back to a git-history draft when the changelog entry is still a placeholder.
- [done] Release notes automation now supports both English and Russian output variants in scripts and workflows.
- [done] Tag-based GitHub Releases now attach the Russian release notes as a release asset and expose a direct link to it in the published release body.
- [done] `prepare_release.sh` now auto-fills new changelog entries with a git-history draft instead of empty `TODO` sections.
- [done] DI/runtime assembly via `NetworkDependencies`.
- [done] UI integration through `NodeFacade`.
- [done] Relay-based reliable messaging pipeline (store/fetch/ack).
- [done] Persistent app storage (secure metadata + chat DB + media filesystem).
- [done] Call stack refactor: extracted `CallNegotiationController` and `CallVideoController` + `CallVideoState`.
- [done] TURN-only call policy for audio/video calls across all network types.
- [done] Chat flow decomposition in UI/state layer (`ChatScreen*`, `ChatController*` split).
- [done] Screen layout structure standardized to `*_screen.dart` + `*_view.dart` + `*_styles.dart`.
- [done] Chat helpers split into dedicated modules (`chat_screen_helpers`, `chat_screen_unread_divider`, `chat_screen_media_actions`).
- [done] Group text/media unified through relay group flow with blob payload references.
- [done] Relay blob chunked upload path added with fallback to single-upload endpoint.
- [done] Server-side membership enforcement added for relay group write path.
- [done] Group membership sync endpoint integrated (`/relay/group/members/update`).
- [done] Group key rotation implemented for membership changes (add/remove).
- [done] Group media crypto payload migrated to compact binary `PLG2` format with legacy decode fallback.
- [done] Large group media crypto moved to background isolate to prevent UI freezes on send/receive.
- [done] Relay fetch GET transient retry/backoff added for unstable connections.
- [done] Bootstrap presence baseline: periodic peer snapshots, online/offline transitions, local `last seen`.
- [done] Self-hosted server deployment flow in Settings with staged progress (`1/14 ... 14/14`) and post-deploy checks for bootstrap/relay/turn.
- [done] Self-hosted endpoints are fixed to `wss://<ip>:443` and `https://<ip>:444` without legacy fallback to `/signal` / `:3000` / `:4000`.
- [done] Self-hosted topology is fixed to HAProxy for `signal/relay` only, while TURN/TURNS is served directly by `coturn`.
- [done] TURN URL normalization in runtime now accepts both `turn:` and `turns:`.
- [done] Runtime bootstrap now keeps multiple WebSocket connections alive simultaneously.
- [done] Outgoing call/WebRTC signaling is multicast to every bootstrap channel where the target peer is visible, with fallback to all connected bootstrap channels.
- [done] Relay runtime now uses a bounded active pool + quorum instead of fan-out to the entire configured server list.
- [done] Relay runtime now preselects only live relay servers, caps active runtime usage at 3 relays, and parallelizes control/blob operations to reduce message/media delays on partially dead relay lists.
- [done] Runtime support for self-signed TLS certificates on IP-hosted bootstrap/relay endpoints.
- [done] Settings server management is now organized through aggregated cards with dedicated bootstrap/relay/turn list screens.
- [done] The `Storage` block in Settings now uses full-card chevron navigation instead of a separate details button.
- [done] Contact avatars now keep a local backup and are restored after app restart before fresh network avatar sync arrives.
- [done] Incoming media restore now has retry/timeout protection for direct blob download plus bounded automatic retry after transient network failure.

## 2. In Progress

- [in_progress] Stabilization of call startup/media flow on mobile networks.
- [in_progress] Cleanup/refactor of large core files into smaller units.

## 3. P0 (Critical)

- [todo] Enable and validate encrypted messaging in runtime (`enableEncryption: true`) with migration-safe behavior.
- [todo] Add integration tests for relay messaging (success/retry/ack/failure cases).
- [todo] Add integration tests for call setup/reconnect after network switch.

## 4. P1 (Core Networking)

- [todo] Decide target strategy for message transport sessions (`direct-only` vs `direct+fallback`).
- [todo] If fallback is required: implement and test `Direct -> TURN -> Relay` in `PeerSession` for message transport.
- [todo] Expand DHT from skeleton to usable lookup/rpc workflows.
- [todo] Reduce dependency on centralized bootstrap signaling beyond the current multi-channel bootstrap runtime.

## 5. P1 (Security)

- [todo] Extend replay protections for control/signaling envelopes where applicable.
- [todo] Extend group-key rotation to full ratcheting/session-key policy.
- [todo] Add negative security tests (invalid signature, replay, tampering).

## 6. P2 (UX/Product)

- [todo] Expand presence UX (privacy controls / richer status) beyond baseline online/last-seen.
- [todo] Delivery/read receipts UX.
- [done] Settings now expose bootstrap/relay/turn availability, sort server rows by state, and use swipe-to-delete with confirmation.

## 7. Quality

- [todo] End-to-end smoke tests: startup/connect/disconnect/background poll.
- [todo] Regression checks for timer/subscription/resource cleanup.
