import 'package:flutter/material.dart';

class SettingsScreenStyles {
  static const EdgeInsets sectionPadding = EdgeInsets.all(18);
  static const EdgeInsets listItemMargin = EdgeInsets.only(bottom: 10);
  static const EdgeInsets emptyLabelPadding = EdgeInsets.symmetric(vertical: 8);

  static const double sectionRadius = 28;
  static const double itemRadius = 22;
  static const double itemHeight = 96;
}
