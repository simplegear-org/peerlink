import 'package:flutter/material.dart';

class QrExportScreenStyles {
  static const EdgeInsets cardPadding = EdgeInsets.all(24);
  static const EdgeInsets qrContainerPadding = EdgeInsets.all(18);

  static const double cardRadius = 28;
  static const double qrContainerRadius = 24;
  static const double qrSize = 240;
}
